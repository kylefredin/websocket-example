const {
  SERVER_HOST = "localhost",
  SERVER_PORT = 3000,
  WEBSOCKET_HOST = "localhost",
  WEBSOCKET_PORT = 8080,
} = process.env;

export { SERVER_HOST, SERVER_PORT, WEBSOCKET_HOST, WEBSOCKET_PORT };
